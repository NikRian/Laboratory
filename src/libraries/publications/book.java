package libraries.publications;

public class book extends publication {
    public book(String title, String author, int year, int quantity){

        super(0, title, author, year, quantity);

    }
}